############################################################
# HighStation Dashboard (Frontend) Environment Variables Example
# 이 파일은 프론트엔드 빌드 및 브라우저 환경 설정을 관리합니다.
# Vite는 'VITE_' 프리픽스가 붙은 변수만 인식합니다.
############################################################

# 1. Supabase 설정 (Dashboard)
# 어디서 얻나요: Supabase Project Settings -> API
# 어디에 쓰이나요: 
#   - 배포 환경: 실제 Supabase Auth 및 실시간 로그 수신에 사용
#   - 로컬 환경: 빌드 에러 방지를 위한 필수값 (실제 동작은 백엔드 프록시를 통함)
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# 2. 애플리케이션 URL 설정
# OAuth 리다이렉트 시 사용할 기본 URL입니다.
# 로컬 개발 시에는 자동으로 window.location.origin이 사용되지만,
# 명시적으로 설정하고 싶은 경우 사용합니다.
VITE_SITE_URL=http://localhost:3000

# [참고] 로컬 개발 시 주의사항
# 로컬에서는 백엔드 서버(3000번 포트)가 프론트엔드를 서빙하므로, 
# 프론트엔드는 /api/... 경로를 통해 백엔드와 통신합니다.
# 다만, Supabase Client 라이브러리 초기화를 위해 위 두 변수는 반드시 정의되어야 합니다.

# 3. 로컬/원격 개발 설정 (Remote Development)
# 로컬 프론트엔드에서 원격(예: N100 홈서버) 백엔드를 바라보려면 아래 변수를 설정하세요.
# 터미널 실행 예시: VITE_API_TARGET=http://192.168.0.100:3000 npm run dev
# 또는 .env.local 파일에 추가하세요.
# VITE_API_TARGET=http://localhost:3000
