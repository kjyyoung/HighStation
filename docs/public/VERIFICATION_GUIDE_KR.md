# 서비스 검증 가이드 (Easy AI Agent 사용)

HighStation 플랫폼의 핵심 가치(서비스 탐색, x402 결제, 무결성 검증)를 가장 쉽고 빠르게 확인하는 방법은 **Easy AI Agent**를 사용하는 것입니다.

## 🤖 Easy AI Agent란?
HighStation 프로토콜(MCP + x402)을 구현한 경량 에이전트 클라이언트입니다. 복잡한 서버 설정 없이도 실제 에이전트의 동작 방식을 시연할 수 있습니다.

---

## 🏗️ 검증 준비

### 1. 지갑 및 자산 준비
- 에이전트는 결제를 위해 **Cronos Testnet USDC.e**가 필요합니다.
- **Faucet**: [https://faucet.cronos.org/](https://faucet.cronos.org/) (USDC 토큰 선택)

### 2. 에이전트 실행
에이전트 디렉토리에서 다음 명령을 실행하십시오.
```bash
cd easy-ai-agent
npm install
npm run start
```

---

## 🧪 검증 시나리오 (Step-by-Step)

### 단계 1: 서비스 탐색 (Discovery)
- 에이전트가 실행되면 상단에 HighStation 게이트웨이와 MCP(Model Context Protocol)로 연결됩니다.
- 현재 이용 가능한 API 서비스 목록이 대시보드와 동기화되어 실시간으로 표시됩니다.

### 단계 2: 서비스 및 엔드포인트 선택
- 목록에서 테스트할 서비스를 선택합니다. (예: `sentence-laundry` 등)
- 해당 서비스가 제공하는 특정 기능(Endpoint)을 선택합니다.

### 단계 3: x402 결제 시연
- API 호출 시 서버가 `402 Payment Required` 응답을 보냅니다.
- 에이전트는 이를 감지하여 **자동으로 결제 서명을 생성**하고 요청을 다시 보냅니다.
- 이 과정에서 지갑 잔액이 차감되며, 실시간 정산 로직이 작동하는 것을 확인할 수 있습니다.

### 단계 4: 결과 확인
- 암호학적 서명(Ed25519)이 포함된 최종 응답 데이터를 확인합니다.
- 플랫폼이 보증하는 **신뢰 점수(Trust Score)**와 데이터 무결성을 체감하십시오.

---

## 💡 주요 검증 포인트
- **결제 자동화**: AI 에이전트가 스스로 결제하고 API를 호출하는 매끄러운 흐름.
- **표준 인터페이스**: MCP를 통한 표준화된 서비스 탐색 및 통합 기술.
- **기술적 무결성**: 모든 통신 과정에서의 보안 및 신뢰성 보장.

> [!TIP]
> 더 자세한 기술 아키텍처는 [Architecture 기술 백서](../public/ARCHITECTURE_KR.md)를 참조하십시오.
